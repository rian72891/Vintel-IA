
-- Add image_analyses_used column if it doesn't already exist (it does per schema)
-- No schema changes needed - tables already have correct structure
-- Just ensure usage_limits has proper defaults
SELECT 1;
